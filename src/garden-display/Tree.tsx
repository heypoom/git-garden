
import styled from '@emotion/styled'

const SIZE = 10

export const Tree = styled.img`
    pointer-events: none;
    position: absolute;
    transform: translate(-3em, -6em);
    width: ${SIZE}em;
    height: ${SIZE}em;
`